ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.erikschouten.cleanarchitecture.server.ServerKt.module ]
    }
    development = true

    database {
    ;     possible values: ["EXPOSED", "LOCAL"]
        type = "EXPOSED"
        schema = "clean-architecture"
        username = "root"
        password = "root"
    }

    jwt {
        domain = "http://localhost/"
        audience = "jwt-audience"
        realm = "ktor sample app"
    }
}
